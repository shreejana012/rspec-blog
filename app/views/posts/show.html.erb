<h2><%= @post.title %> by <%= @post.author %> </h2>
	<p>
	<%= @post.content %>
    </p>
    <%= link_to 'Edit',edit_post_path, class: 'btn btn-sm btn-success' %>
      <%= link_to 'Delete', post_path, method: :delete, class: 'btn btn-sm btn-danger', data: {confirm: "Are you sure?"} %>
